[phases.setup]
NODE_VERSION = "20"
nixPkgs = ["chromium"]

[phases.install]
cmds = ["npm install"]
env = { PUPPETEER_SKIP_DOWNLOAD = "true" }

[phases.build]
# kosongkan saja
cmds = []

[start]
cmd = "node bot.js"
